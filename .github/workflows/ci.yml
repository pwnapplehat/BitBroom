name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

# Releases are cut manually from locally built + smoke-tested artifacts
# (see docs/BUILDING.md → "Cutting a release"). CI validates every push/PR/tag
# and publishes the binaries as downloadable build artifacts.
jobs:
  build-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore BitBroom.sln

      - name: Build
        run: dotnet build BitBroom.sln -c Release --no-restore

      - name: Test
        run: dotnet test tests/BitBroom.Core.Tests/BitBroom.Core.Tests.csproj -c Release --no-build --verbosity normal

      - name: Publish (win-x64)
        run: ./build/publish.ps1 -Runtime win-x64
        shell: pwsh

      - name: Publish (win-arm64)
        run: ./build/publish.ps1 -Runtime win-arm64
        shell: pwsh

      - name: Upload artifacts (x64)
        uses: actions/upload-artifact@v4
        with:
          name: BitBroom-win-x64
          path: dist/win-x64/

      - name: Upload artifacts (arm64)
        uses: actions/upload-artifact@v4
        with:
          name: BitBroom-win-arm64
          path: dist/win-arm64/
