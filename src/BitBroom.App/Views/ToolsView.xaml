<UserControl x:Class="BitBroom.App.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:anim="clr-namespace:BitBroom.App.Animations"
             anim:Anim.Transition="SlideFade">
  <Grid Margin="28,24,28,20" DataContext="{Binding Tools}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="Tools" Style="{StaticResource Text.Title}"/>
      <TextBlock Style="{StaticResource Text.Subtitle}" Margin="0,6,0,0"
                 Text="Supported, documented maintenance actions. Each runs the official Windows mechanism (DISM, powercfg, vssadmin) and shows its output below."/>
    </StackPanel>

    <WrapPanel Grid.Row="1" Margin="0,16,0,0">
      <WrapPanel.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource Button.Base}">
          <Setter Property="Margin" Value="0,0,10,10"/>
          <Setter Property="Padding" Value="14,10"/>
        </Style>
      </WrapPanel.Resources>
      <Button Command="{Binding FlushDnsCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE704;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Accent}"/>
          <TextBlock Text="Flush DNS cache" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
      <Button Command="{Binding RestartExplorerCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE777;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Accent}"/>
          <TextBlock Text="Restart Explorer" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
      <Button Command="{Binding AnalyzeWinSxSCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE9D9;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Accent}"/>
          <TextBlock Text="Analyze component store" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
      <Button Command="{Binding CleanWinSxSCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xEA99;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Accent}"/>
          <TextBlock Text="Component store cleanup (DISM)" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
      <Button Command="{Binding ListShadowStorageCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE81C;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Accent}"/>
          <TextBlock Text="System Restore usage" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
      <Button Command="{Binding DisableHibernationCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE708;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Warning}"/>
          <TextBlock Text="Disable hibernation" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
      <Button Command="{Binding ReduceHibernationCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE73F;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Warning}"/>
          <TextBlock Text="Shrink hiberfil (keep Fast Startup)" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
      <Button Command="{Binding EnableHibernationCommand}">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE945;" FontFamily="{StaticResource Font.Icons}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Brush.Success}"/>
          <TextBlock Text="Enable hibernation" Margin="8,0,0,0"/>
        </StackPanel>
      </Button>
    </WrapPanel>

    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="4" Margin="0,6,0,0">
      <ScrollViewer x:Name="ConsoleScroll" VerticalScrollBarVisibility="Auto">
        <TextBox Text="{Binding ConsoleText, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap"
                 FontFamily="Cascadia Mono, Consolas" FontSize="12"
                 Background="Transparent" BorderThickness="0" Foreground="{StaticResource Brush.TextSecondary}"
                 TextChanged="OnConsoleTextChanged"/>
      </ScrollViewer>
    </Border>
  </Grid>
</UserControl>
