<UserControl x:Class="BitBroom.App.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:anim="clr-namespace:BitBroom.App.Animations"
             anim:Anim.Transition="SlideFade">
  <Grid Margin="28,20,28,20" DataContext="{Binding Tools}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="Tools" Style="{StaticResource Text.Title}"/>
      <TextBlock Style="{StaticResource Text.Subtitle}" Margin="0,6,0,0"
                 Text="Supported, documented maintenance actions. Each runs the official Windows mechanism (DISM, powercfg, vssadmin) and shows its output below."/>
    </StackPanel>

    <WrapPanel Grid.Row="1" Margin="0,16,0,0">
      <WrapPanel.Resources>
        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
          <Setter Property="Margin" Value="0,0,10,10"/>
        </Style>
      </WrapPanel.Resources>
      <ui:Button Content="Flush DNS cache" Command="{Binding FlushDnsCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="Globe24"/>
        </ui:Button.Icon>
      </ui:Button>
      <ui:Button Content="Restart Explorer" Command="{Binding RestartExplorerCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="ArrowClockwise24"/>
        </ui:Button.Icon>
      </ui:Button>
      <ui:Button Content="Analyze component store" Command="{Binding AnalyzeWinSxSCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="DocumentSearch24"/>
        </ui:Button.Icon>
      </ui:Button>
      <ui:Button Content="Component store cleanup (DISM)" Command="{Binding CleanWinSxSCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="Broom24"/>
        </ui:Button.Icon>
      </ui:Button>
      <ui:Button Content="System Restore usage" Command="{Binding ListShadowStorageCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="History24"/>
        </ui:Button.Icon>
      </ui:Button>
      <ui:Button Content="Disable hibernation" Command="{Binding DisableHibernationCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="WeatherMoon24"/>
        </ui:Button.Icon>
      </ui:Button>
      <ui:Button Content="Shrink hiberfil (keep Fast Startup)" Command="{Binding ReduceHibernationCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="ArrowMinimize24"/>
        </ui:Button.Icon>
      </ui:Button>
      <ui:Button Content="Enable hibernation" Command="{Binding EnableHibernationCommand}">
        <ui:Button.Icon>
          <ui:SymbolIcon Symbol="Flash24"/>
        </ui:Button.Icon>
      </ui:Button>
    </WrapPanel>

    <ui:Card Grid.Row="2" Padding="4" Margin="0,6,0,0" VerticalContentAlignment="Stretch">
      <ScrollViewer x:Name="ConsoleScroll" VerticalScrollBarVisibility="Auto">
        <TextBox Text="{Binding ConsoleText, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap"
                 FontFamily="Cascadia Mono, Consolas" FontSize="12"
                 Background="Transparent" BorderThickness="0"
                 Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                 TextChanged="OnConsoleTextChanged"/>
      </ScrollViewer>
    </ui:Card>
  </Grid>
</UserControl>
